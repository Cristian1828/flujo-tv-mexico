---
import { testimonials } from '../config/site';
---

<section id="testimonios" class="relative py-16 md:py-24">
  <div class="pointer-events-none absolute inset-0 -z-10">
    <div class="absolute right-0 top-8 h-56 w-56 rounded-full bg-rose-600/10 blur-3xl"></div>
  </div>
  <div class="container-main surface-panel px-4 py-10 md:px-8 md:py-12">
    <!-- Encabezado -->
    <div class="mx-auto max-w-2xl text-center">
      <span class="text-xs font-bold uppercase tracking-[0.2em] text-amber-400">Testimonios</span>
      <h2 class="mt-3 font-display text-3xl font-bold text-white md:text-4xl lg:text-5xl">
        Lo que dicen nuestros usuarios
      </h2>
      <p class="mt-4 text-base text-zinc-100">
        Usuarios de Mexico ya disfrutan Flujo TV todos los dias. Esto es lo que opinan.
      </p>
    </div>

    <div class="line-accent mx-auto mt-8 w-24"></div>

    <!-- Métricas de confianza -->
    <div class="mt-10 grid grid-cols-3 gap-4 mx-auto max-w-lg">
      <div class="card-dark py-4 text-center">
        <p class="font-display text-2xl font-bold text-white">+10K</p>
        <p class="text-[10px] font-medium uppercase tracking-widest text-zinc-100/85">Usuarios Activos</p>
      </div>
      <div class="card-dark py-4 text-center">
        <p class="font-display text-2xl font-bold text-white">93%</p>
        <p class="text-[10px] font-medium uppercase tracking-widest text-zinc-100/85">Satisfacción</p>
      </div>
      <div class="card-dark py-4 text-center">
        <p class="font-display text-2xl font-bold text-white">24/7</p>
        <p class="text-[10px] font-medium uppercase tracking-widest text-zinc-100/85">Soporte</p>
      </div>
    </div>

    <!-- Grid de testimonios -->
    <div class="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
      {testimonials.map((t) => (
        <article class="card-dark-hover overflow-hidden">
          <!-- Foto -->
          <div class="relative h-48 overflow-hidden">
            <img
              src={t.image}
              alt={t.name}
              class="h-full w-full object-cover"
              loading="lazy"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-dark-800/95 via-dark-800/30 to-transparent"></div>
            <!-- Estrellas -->
            <div class="absolute bottom-3 left-4 flex gap-0.5">
              {Array.from({ length: t.rating }).map(() => (
                <svg class="h-3.5 w-3.5 text-amber-400" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                </svg>
              ))}
            </div>
          </div>
          <!-- Texto -->
          <div class="p-5">
            <p class="text-sm leading-relaxed text-zinc-200">"{t.text}"</p>
            <div class="mt-4">
              <p class="text-sm font-bold text-white">{t.name}</p>
              <p class="text-xs text-zinc-100/80">{t.location}</p>
            </div>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>
